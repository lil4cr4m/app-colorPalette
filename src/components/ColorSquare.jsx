
// Create the component to display one color and receive the colorHex prop.

import React from "react";

// The Output Unit. Displays a single hex code color block.
const ColorSquare = ({colorHex}) => {
  return <div></div>;
};

export default ColorSquare;
