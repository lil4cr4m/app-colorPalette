// Create the UI for file selection and the 'Random' button. 
// This component will pass the image data/action up to its parent.

import React from 'react';

const ImageUpload = () => {
  return (
    <div>
      
    </div>
  );
};

export default ImageUpload;